<% content_for :quran_text do %>
    <% @ayat.each do |aya| %>
        <span id="19-2" class="aya">
            <span class="aya-wrapper" style="background-color: transparent;">
                <span class="ayaText">
                  <!--<%= aya.text %>-->
                  <% aya.text.split(/\s+/).each do |word| %>
                    <span class="ayaWord"><%= word.to_s %></span>
                  <% end %>
                </span>
                <span class="ayaNumber" style="font-size: 0.92em;">﴿<a href="http://tanzil.net/#19:2">٢</a>﴾</span>
            </span>
        </span>
    <% end %>
<% end %>

<% content_for :atks_sarf do %>
    <h2>Page: <%= @page %></h2>
    <br/>
    <h2>nextPageSura: <%= @nextPageSura %></h2>
    <br/>
    <h2>nextPageAya: <%= @nextPageAya %></h2>
<% end %>
